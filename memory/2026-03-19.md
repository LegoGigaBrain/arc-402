# 2026-03-19

- Shipped first ARC-402 endpoint CLI scaffold under `arc402 endpoint`.
- Added persisted endpoint config at `~/.arc402/endpoint.json` so canonical public identity is stored separately from daemon/OpenShell state.
- `arc402 endpoint init` now scaffolds `agentname.arc402.xyz` + local ingress target using launch-default host-managed Cloudflare Tunnel language.
- `arc402 endpoint status` and `arc402 endpoint doctor` now distinguish runtime sandbox health from ingress/tunnel health instead of blurring them.
- `arc402 endpoint claim <agentname> --tunnel-target <https://...>` now claims the hostname and locks local config, but AgentRegistry endpoint update remains a separate explicit step for now.
- Remaining endpoint work: DNS/AgentRegistry parity checks, peer allow/revoke helpers, and fuller tunnel introspection.
